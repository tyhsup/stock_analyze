您好。根據您的指示，我已準備就緒進行 `multi-agent-council MCP Server` 的修復與註冊流程分析。

由於這是一個涉及核心服務層（Service Layer）的架構級任務，為了確保修復的精準性、穩定性，並避免引入新的系統依賴衝突，請您提供以下資訊：

1.  **現有程式碼庫 (Codebase)：** 包含 `multi-agent-council` 的定義文件、伺服器啟動腳本（例如 `run_server.py`）以及相關的配置檔。
2.  **錯誤日誌 (Error Logs)：** 請提供執行失敗時捕獲到的完整堆疊追蹤 (Stack Trace) 和任何 HTTP 狀態碼回傳訊息，這對於定位故障點至關重要。
3.  **功能規格書 (Spec Sheet)：** 確認 `multi-agent-council` 的核心職責、預期的 API 端點（Endpoints）以及它與其他模組（例如數據存取層 Data Access Layer）的互動流程圖。

---

### 🛠️ 架構分析與修復原則 (Architectural Analysis & Repair Principles)

在您提供程式碼後，我將會從以下三個維度進行系統性的審查與重構：

#### 1. 服務註冊與依賴管理 (Service Registration & Dependency Management)
*   **目標：** 確認 `MCP Server` 的啟動流程是否遵循標準的微服務（Microservice）模式，確保其能夠在現有的容器環境中被正確發現和調度。
*   **檢查重點：** 資源初始化順序、配置變數 (Environment Variables) 的載入機制，以及是否有硬編碼的 IP 或 Port 資訊。

#### 2. 核心邏輯與狀態管理 (Core Logic & State Management)
*   **目標：** 修復導致服務崩潰或行為異常的程式碼路徑。
*   **檢查重點：**
    *   **異步處理 (Asynchronous Handling)：** 確認多個 Agent 的協調機制是否使用了正確的鎖定（Locking）和訊息佇列（Message Queue），避免 Race Condition。
    *   **錯誤邊界 (Error Boundaries)：** 確保任何單一 Agent 的失敗不會導致整個 `MCP Server` 服務層崩潰，必須實施強健的 `try...except` 區塊。

#### 3. 資料傳輸與序列化保護 (Data Transfer & Serialization Protection)
*   **【關鍵防禦點】**：由於此伺服器必然涉及數據存取和 API 通訊，我會特別檢查以下四個「專案歷史踩坑防禦要點」的執行情況。

---

### 🛡️ 【專案歷史踩坑防禦要點】強制審查清單 (Mandatory Defense Checklist)

在修復或新增任何涉及 I/O、API 通訊或資料庫操作的程式碼時，我將會主動檢查並確保以下規則被嚴格遵守：

1.  **【Unicode 防禦】：** 任何讀取本地檔案（如配置檔、模型權重）的操作，必須明確使用 `open(..., encoding='utf-8', errors='ignore')` 或 `errors='replace'` 來處理編碼衝突。
2.  **【JSON 序列化保護】：** 所有從資料庫 (MySQL) 查詢的結果集，在傳回給 API 層或外部服務前，必須執行數據類型轉換：
    *   `datetime.date`/`datetime.datetime` $\rightarrow$ 標準 `string` (ISO 8601 format)。
    *   `decimal.Decimal` $\rightarrow$ 標準 `float` 或 `string`。
3.  **【Gemini API 400 兼容】：** 如果 Agent 的推理過程涉及呼叫 Gemini API，在執行 Function Calling 的第二輪回傳時（即 `functionResponse`），必須將第一輪接收到的完整內容 (包含原始的 `thoughtSignature`) 原封不動地放入 `contents` 的 `model` 角色中。
4.  **【零破壞原則】：** 所有新的監控介面或管理功能，都必須設計為獨立的、可選配的 App 或 Dashboard 組件，絕不能修改或遮擋現有的 K 線圖繪製邏輯與核心估值計算流程。

---

請您提供相關程式碼和日誌，我將立即開始進行精準的代碼審查與架構修復工作。