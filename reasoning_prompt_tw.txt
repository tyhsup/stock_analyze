我們需要分析為什麼台灣股票 00981A.TW (以及其他類似的 ETF / 特別股等有價證券) 沒有被成功抓取。

目前代碼：
1. `update_tw_stocks.py` 只有在 `current_type == "股票"` 時才抓取。而 00981A 的類別在 isin 網頁是 `ETF`。
2. `update_tw_prices.py` 使用 `yf.download(..., period="5d")`。這意味著新加入的股票即使被加入了，也只有 5 天的歷史資料，無法補齊所有歷史資料。

請提供：
1. 如何修改 `update_tw_stocks.py` 以抓取 `['股票', '特別股', '創新板', 'ETF', '臺灣存託憑證(TDR)']`？
2. 如何修改 `update_tw_prices.py`，使它能智能檢查：如果資料庫中該股票在 `DailyPriceTW` 沒有任何價格資料，則使用 `period="max"` 或是較長的 period (例如 `period="1y"`) 下載歷史價格，補齊歷史資料？而對於已有資料的股票，則繼續使用 `period="5d"` 進行增量更新。
3. 請確保遵守：變數命名使用英文、語意清晰、無冗餘代碼。
